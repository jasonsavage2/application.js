/**
 * Module : RegistrationForm.js
 *
 * Setups up all client site validation for the registration form.
 *
 */

( function(app)
{
	app.module("registrationForm", {

		requires : [
			"jquery-1.9.1.min",
			"jquery.validate.min"
		],

		init : function()
	    {

	    	
		}
	});

}(app));

